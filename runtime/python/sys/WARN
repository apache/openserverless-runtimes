==============================================================================
WARN -- Apache License 2.0 incompatibilities and open review items
Container image: apache/openserverless-runtime-python:sys
==============================================================================

This file accompanies the LICENSE and NOTICE files beside it. Everything below
is a WARNING: it is NOT part of the licensing grant. Each item must be
resolved or confirmed by the PPMC and the Incubator mentors before a release
vote.

The inventory it comments on is the 29 Python distributions pinned in
requirements.txt beside this file, plus the base image and the packages the
Dockerfile installs from the Debian archive.

Classification follows the ASF third-party licensing policy:

    https://www.apache.org/legal/resolved.html

  Category A  Apache-2.0 compatible. May be included in a release.
  Category B  May be included in BINARY form only, unmodified and as a
              separate file, with the reciprocal license disclosed.
  Category X  MUST NOT be included in an Apache release artifact.


SCOPE NOTE
------------------------------------------------------------------------------
The Apache release artifact for OpenServerless is the SOURCE distribution,
which contains this Dockerfile and the files beside it -- all Apache-2.0
licensed. The published container image is a CONVENIENCE BINARY assembled at
build time from third-party base images and package repositories. Category X
components inherited from a base operating system image are disclosed below
but are not redistributed by the ASF in source form. Category X components
that this Dockerfile ADDS on purpose must be removed.


==============================================================================
CATEGORY X (AS BUNDLED)
==============================================================================

  Component : Debian bookworm base image userland
              (via python:3.12.14-bookworm)
  License   : mixed, incl. GPL-2.0/GPL-3.0
  Category  : X (as bundled)
  Action    :
      The base image contains GPL-licensed operating-system components. The
      Apache release artifact is the SOURCE (this Dockerfile and the files
      beside it); the published container image is a convenience binary
      assembled from third-party base images. No action required beyond this
      disclosure.


==============================================================================
CATEGORY B -- WEAK COPYLEFT, BINARY INCLUSION ONLY
==============================================================================

  Component : certifi 2026.7.22
  License   : MPL-2.0
  Category  : B
  Action    :
      Pulled in transitively by requests, httpx, minio and others. Binary form
      only, unmodified, as a separate file, with the license disclosed. See
      warning 4 below: certifi itself ships only a pointer to the MPL, not a
      copy of it.


==============================================================================
LICENSES NEEDING A JUDGEMENT CALL
==============================================================================

  Component : pycryptodome 3.23.0
  License   : BSD-2-Clause AND public domain
  Category  : A
  Action    :
      Code originating from PyCrypto is released into the public domain, the
      remainder is BSD 2-clause. Both are permissive. The distribution names
      its contributors in an AUTHORS file rather than in a single copyright
      line, which is why LICENSE points at that file instead of quoting one.

  Component : cffi 2.1.1 and scramp 1.4.17
  License   : MIT-0 (MIT No Attribution)
  Category  : A
  Action    :
      MIT-0 drops the attribution requirement of MIT. It is more permissive
      than MIT, not less, so nothing is required of a redistributor. Recorded
      for completeness.


==============================================================================
CATEGORY A
==============================================================================

  Component : CPython 3.12.14 and the components bundled with it
  License   : PSF-2.0, with bundled code under further permissive licenses
  Category  : A
  Action    :
      No action required.

  Component : zip (Info-ZIP), python3-lxml (BSD-3-Clause), plus the 29 Python
              distributions pinned in requirements.txt
  License   : MIT family (13), Apache-2.0 (9), BSD (4), MPL (see Category B)
              (2), ISC (1), PSF (1)
  Category  : A
  Action    :
      No action required. Every distribution is recorded one by one in LICENSE
      with its version, license and copyright notice.

  Component : Go modules linked into /bin/proxy
  License   : BSD-3-Clause, MIT, ISC, Apache-2.0
  Category  : A
  Action    :
      No action required. See warning 10.



==============================================================================
SUMMARY OF REQUIRED REMOVALS (Category X added by this Dockerfile)
==============================================================================

  None. This Dockerfile adds no Category X component. Unlike the sibling
  Python runtimes, this image installs neither numpy nor scipy, so no
  LGPL-licensed libquadmath shared object is present.

  The GPL components of the Debian userland are inherited from the base
  image.


==============================================================================
WARNINGS ON THE INVENTORY ITSELF
==============================================================================

  1. THE INVENTORY DESCRIBES THE linux/amd64 WHEELS
     Where a distribution publishes platform wheels, the license facts in
     LICENSE were taken from the manylinux x86_64 wheel, because that is what
     pip installs in this image. An image built for linux/arm64 would resolve
     to different wheels, and its vendored-library set has NOT been checked
     here.

  2. certifi SHIPS A POINTER TO THE MPL, NOT A COPY OF IT
     certifi's license file is a short notice saying the Source Code Form is
     subject to the MPL 2.0 and giving a URL. The MPL requires that a copy of
     the license accompany the distribution. Since this LICENSE records
     licenses by reference rather than reproducing their full texts, a full
     MPL-2.0 copy is NOT present in the source tree. Decide with the PPMC
     whether the reference is sufficient for a convenience binary, or add the
     full MPL-2.0 text to the release.

  3. THREE DISTRIBUTIONS ARE INSTALLED WITHOUT ANY VERSION CONSTRAINT
     The build runs

         pip3 install --upgrade pip six wheel virtualenv

     before installing the pinned requirements. six, wheel and virtualenv --
     and virtualenv's own dependencies -- are therefore whatever the index
     serves on the day of the build, and are NOT part of the inventory in
     LICENSE except for six, which requirements.txt also pins. The two steps
     contradict each other, exactly as they do in the sibling Python runtimes.
     Pin them, or drop them and let requirements.txt decide.

  4. THE PINNED REQUIREMENTS ARE DETERMINISTIC, THE INVENTORY IS NOT
     SELF-MAINTAINING
     requirements.txt is generated by pip-compile from requirements.in and
     pins all 29 distributions with ==, so the inventory is exact for as long
     as that file is not regenerated. Regenerating it can introduce new
     transitive distributions whose licenses nobody has reviewed. Add a license
     check to whatever regenerates requirements.txt, and regenerate LICENSE,
     NOTICE and this file in the same step.

  5. VENDORED BINARY CODE IS ONLY AS VISIBLE AS THE WHEEL MAKES IT
     A wheel is under no obligation to declare the shared objects it vendors.
     argon2-cffi-bindings, bcrypt, cffi, pycryptodome and others in this image
     ship compiled code built from vendored C and Rust projects, and no claim
     is made here that every vendored component is accounted for.

  6. WHEEL METADATA WAS READ FROM PyPI, NOT FROM A BUILT IMAGE
     The license facts recorded in LICENSE were taken from the wheels and
     sdists published on PyPI at exactly the versions pinned in
     requirements.txt. The authoritative copy for the shipped image remains
     the <dist>-<version>.dist-info/ directory inside the image.

  7. THE BUNDLED PROXY CANNOT BE VERIFIED FROM THIS DIRECTORY
     /bin/proxy is copied from the image named by the COMMON build argument,
     which defaults to `missing:missing`. The Go module inventory was derived
     from the go.mod and go.sum of this repository, not from the binary that
     actually ends up in the image.

  8. LICENSE RECORDS LICENSES BY REFERENCE, NOT BY REPRODUCTION
     Following the layout of the Apache Pekko LICENSE and NOTICE

         https://github.com/apache/pekko/blob/main/LICENSE
         https://github.com/apache/pekko/blob/main/NOTICE

     each component block names the license and reproduces the copyright
     notice, rather than reproducing the full license text. This satisfies the
     attribution requirement of the MIT and BSD families, whose condition is
     that the copyright notice be retained. Confirm with the mentors that it is
     also acceptable for the MPL components in Category B above.

  9. DEPENDENCIES OF USER ACTIONS ARE NOT COVERED
     Beyond the distributions listed above, user actions bring their own
     Python dependencies at deploy time. Those libraries are NOT part of this
     image and NOT part of any Apache release artifact, and no claim is made
     here about their licenses.


==============================================================================
ADDITIONAL NOTES
==============================================================================

This image is the ":sys" runtime used by the internal OpenServerless system
actions. It ships to every installation, so its inventory is the one with the
widest reach, and its requirements.in deliberately carries only the default
package set shared by all the Python runtimes -- no machine-learning or LLM
libraries.

lxml arrives here as the Debian python3-lxml package, not as a wheel.
